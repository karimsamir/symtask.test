<nav class="navbar navbar-expand-lg navbar-dark bg-primary">

  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01"
    aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">

    {% if is_granted('ROLE_USER') %}

      <li class="nav-item active">
        <a class="nav-link" href="{{ path("quiz_homepage") }}">Quiz <span class="sr-only">(current)</span></a>
      </li>
    {% endif %}

    {% if is_granted('ROLE_ADMIN') %}

      <li class="nav-item active">
        <a class="nav-link" href="{{ path("user_index") }}">
        Users
        <span class="sr-only">(current)</span>
        </a>
      </li>
    {% endif %}

      {% if app.user %}
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true"
          aria-expanded="false">
          Welcome {{ app.user.email }}
        </a>
        <div class="dropdown-menu">
          <a class="dropdown-item" href="{{ path("app_logout") }}">Logout</a>
        </div>
      </li>

      {% else %}

      <li class="nav-item active float-right">
        <a class="nav-link" href="{{ path("app_login") }}">Login <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item active float-right">
        <a class="nav-link" href="{{ path("app_register") }}">Register <span class="sr-only">(current)</span></a>
      </li>

      {% endif %}
      {# <li class="nav-item">
        <a class="nav-link" href="#">Features</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Pricing</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">About</a>
      </li> #}
      {# <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Dropdown</a>
        <div class="dropdown-menu">
          <a class="dropdown-item" href="#">Action</a>
          <a class="dropdown-item" href="#">Another action</a>
          <a class="dropdown-item" href="#">Something else here</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="#">Separated link</a>
        </div>
      </li> #}
    </ul>

  </div>
</nav>